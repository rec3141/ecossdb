name: ecossdb-core
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.10
  # Only openpyxl is non-stdlib; needed for parse_cices.py (CICES xlsx → TSV).
  # All pipeline runtime scripts (map_to_es, aggregate_*, score_es, etc.)
  # use only stdlib (csv, json, argparse, collections) — no extra deps needed.
  - openpyxl>=3.1
